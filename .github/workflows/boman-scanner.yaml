# This is a basic workflow to help you get started with boman.ai

name: Bomanai Scanner
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  Boman-scan:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Installs Boman Package and runs scan on code repository
      - name: Installing Boman
        run: sudo pip install boman-cli
      - name: Run scan
        env:
          APP: ${{ secrets.APP_TOKEN }}
          USER: ${{ secrets.CUSTOMER_TOKEN }}
        run: sudo boman-cli -a run -at be2ade3d-f945-4516-8c4d-90d00bdd798e -ct 4c21d6a7-f519-40bf-9cc2-9f5377a44230
